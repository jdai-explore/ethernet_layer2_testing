**Review Report:**


Current test run: `47 passed, 8 warnings` using `PYTHONDONTWRITEBYTECODE=1` and pytest cache disabled. `git status` stayed clean.

**Overall verdict:** the project has the shape of the requested architecture and all 71 spec IDs are represented in YAML, but the implementation is not yet a conformance-grade TC8 Layer 2 test framework. The largest gap is that many specs are represented as metadata or generic smoke behavior, not as spec-accurate automotive Ethernet procedures.

::code-comment\{title="\[P1\] Spec parameters are mostly ignored" body="The generator expands only VID, frame type, TPID, and port pairs. YAML parameters such as pcp\_values, payload\_sizes, ethertypes, rates\_pps, mac\_count, outer\_vid\_range, and inner\_vid\_range do not become concrete test cases, so many TC8 clauses are not actually exercised even though their spec IDs are loaded." file="Z:\\layer2\_Ethernet\_Testing\\ethernet\_layer2\_testing\\tc8-l2-test-framework\\src\\core\\test\_runner.py" start=73 end=99 priority=1 confidence=0.95\} ::code-comment\{title="\[P1\] Sessions are shared across all cases in a spec" body="The PRD says every test must execute in an isolated session, but the runner creates one session per spec group and executes all generated cases inside it. MAC learning, aging, VLAN state, counters, and storm-control state can leak between cases." file="Z:\\layer2\_Ethernet\_Testing\\ethernet\_layer2\_testing\\tc8-l2-test-framework\\src\\core\\test\_runner.py" start=261 end=277 priority=1 confidence=0.9\} ::code-comment\{title="\[P1\] Address learning is not actually learned before validation" body="SWITCH\_ADDR\_001 says it should learn a source MAC and then send to that MAC from another port, but the handler performs only one send\_and\_capture call and validates expected egress ports. That cannot prove source learning, learned forwarding, migration, or no-learning behavior." file="Z:\\layer2\_Ethernet\_Testing\\ethernet\_layer2\_testing\\tc8-l2-test-framework\\src\\specs\\address\_tests.py" start=84 end=101 priority=1 confidence=0.95\} ::code-comment\{title="\[P2\] Web progress callback is never wired" body="The async ws\_progress callback is declared but not passed into TestRunner, and TestRunner invokes callbacks synchronously. The web UI therefore cannot receive real-time progress as implemented." file="Z:\\layer2\_Ethernet\_Testing\\ethernet\_layer2\_testing\\tc8-l2-test-framework\\web\\backend\\main.py" start=236 end=252 priority=2 confidence=0.9\} ::code-comment\{title="\[P2\] Double-tag TPID is hardcoded" body="Double-tagged frame construction always uses 0x88A8 for the outer TPID and 0x8100 for the inner TPID instead of honoring case parameters or YAML values. Legacy/custom TPID coverage, including 0x9100, is therefore not real." file="Z:\\layer2\_Ethernet\_Testing\\ethernet\_layer2\_testing\\tc8-l2-test-framework\\src\\interface\\scapy\_interface.py" start=313 end=320 priority=2 confidence=0.9\}

| Rank | Pareto | Issue | Root cause | Proposal |
| -: | - | - | - | - |
| 1 | Top 20% | Spec YAML is loaded but not faithfully expanded into test cases: [test\_runner.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/src/core/test_runner.py:73), [test\_case.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/src/models/test_case.py:167) | `TestSpecDefinition` keeps most YAML under untyped `parameters`; generator only reads `vid\_range`, default frame types, TPIDs, and ports. | Add typed parameter expansion per spec family: PCP, DEI, TPID, payload size, EtherType, MAC count, rates, outer/inner VLANs. Add tests asserting generated cases for representative specs. |
| 2 | Top 20% | Many TC8 specs are generic or informational rather than executable conformance tests: [address\_tests.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/src/specs/address_tests.py:153), [qos\_tests.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/src/specs/qos_tests.py:75), [config\_tests.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/src/specs/config_tests.py:75) | Spec IDs were scaffolded before full test procedures and measurement mechanisms existed. | Split “implemented”, “informational”, and “not automated” status per spec. Build real procedures first for VLAN, address learning, filtering, QoS PCP preservation. |
| 3 | Top 20% | Test isolation contradicts PRD: one session per spec, not per test case: [test\_runner.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/src/core/test_runner.py:261) | Optimization was introduced by grouping cases, but L2 tests are stateful. | Default to per-case setup/teardown. Allow per-spec session reuse only for explicitly stateful multi-step specs. |
| 4 | Top 20% | Address learning tests do not perform learn-then-probe sequences: [address\_tests.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/src/specs/address_tests.py:93) | `\_send\_and\_capture` is used as a single-step abstraction for multi-step behaviors. | Implement procedure objects: learn source on port A, verify unknown flooding, verify learned unicast, move MAC, wait aging, re-probe. |
| 5 |  | Validator misses many expected-result keys and can pass unvalidated semantics: [result\_validator.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/src/core/result_validator.py:90) | Checks cover forwarding, basic VLAN tags, counts, drop, but ignore keys like `learned\_port`, `registered\_ports`, `pcp\_preserved`, `rate\_limited`, `residence\_time\_corrected`. | Make expected-result schema explicit and fail/skip when a result key has no validator. |
| 6 |  | VLAN handlers mutate generated test cases during execution: [vlan\_tests.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/src/specs/vlan_tests.py:120) | Handlers compensate for bad generation by changing `case.parameters`. | Generate correct immutable parameters up front, or copy cases before mutation. |
| 7 |  | Double-tagging coverage is incomplete: [scapy\_interface.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/src/interface/scapy_interface.py:315) | TPIDs and inner VID/PCP are hardcoded/defaulted. | Drive outer TPID, inner TPID, outer VID, inner VID, PCP, DEI from generated case parameters. |
| 8 |  | Runt frame test cannot create runt frames: [general\_tests.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/src/specs/general_tests.py:311) | `payload\_size` minimum is 46, which generally yields a legal 64-byte Ethernet frame without FCS. | Use raw frame injection capable of sending \<64-byte frames or mark as requires special hardware/NIC support. |
| 9 |  | WebSocket progress is not functional: [main.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/web/backend/main.py:236) | Callback not passed; callback type mismatch. | Pass callback and support async callbacks in `TestRunner`, or make web progress sync-safe via queue. |
| 10 |  | Tests pass but mostly verify scaffolding, not TC8 behavior: [test\_smoke\_suite.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/tests/integration/test_smoke_suite.py:61) | Assertions check “report exists” and “spec count is 71”, not expected pass/fail semantics. | Add negative tests using fake interfaces: missing forwarding must fail, VLAN leakage must fail, PCP mismatch must fail, aging not waited must not pass. |
| 11 |  | `tests/verify\_report\_viz.py` is an executable script with side effects, not pytest: [verify\_report\_viz.py](Z:/layer2_Ethernet_Testing/ethernet_layer2_testing/tc8-l2-test-framework/tests/verify_report_viz.py:1) | It runs at import/script execution and writes reports/DB files. | Convert to pytest tests using `tmp\_path` and in-memory SQLite. |
| 12 |  | Current `test\_output.txt` is stale and contradicts the passing suite. | The file records an older run with 3 failures, while current tests pass. | Remove generated test output from source control or regenerate it deliberately in CI artifacts only. |


**Architecture/Spec Check** The repo follows the intended folder architecture: core runner/config/session, section-specific specs, interface, reporting, web UI, YAML spec definitions. The spec inventory is complete by ID count: 21 VLAN, 10 General, 21 Address, 11 Filtering, 1 Time, 4 QoS, 3 Config.

The implementation is not yet “as per specification” in the behavioral sense. It is closer to a scaffold/demo framework with simulation paths and partial real Scapy send/capture than a TC8 L2 conformance runner.

**Testing Check** Current suite passes: `47 passed, 8 warnings`. Coverage quality is limited. It validates loading, basic report rendering, hexdump utilities, registry existence, and a simulation smoke path. It does not validate real DUT behavior, most failure modes, parameter expansion, negative cases, timing accuracy, QoS ordering, filtering rates, or address-table state transitions.
